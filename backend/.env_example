# Backend Environment Variables

# For Production (PostgreSQL - these will be set on your hosting platform)
# NODE_ENV=production
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE_NAME

# For Local Development with PostgreSQL (uncomment and replace with your local PG details if you set one up)
# NODE_ENV=development
# DATABASE_URL=postgres://your_local_pg_user:your_local_pg_password@localhost:5432/your_local_pg_db_name

# For Local Development with SQLite (current default if DATABASE_URL is not set and NODE_ENV is not production)
# No specific variables needed if falling back to SQLite config in database.js

# Application Port (Optional - hosting platforms usually set this)
# PORT=3001

# JWT Secret Key (REQUIRED - generate a strong random string)
JWT_SECRET=your_jwt_secret_key_here

# Other backend specific environment variables can be added here
# e.g., email service credentials, API keys for external services, etc.
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback # Change in production

EMAIL_HOST=your_email_host
EMAIL_PORT=your_email_port
EMAIL_USER=your_email_user
EMAIL_PASS=your_email_pass
EMAIL_FROM=your_email_from_address
